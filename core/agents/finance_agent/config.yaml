name: "finance_agent"
description: "Handles invoice extraction, financial validation, and ledger arithmetic."
llm_tier: "worker"
temperature: 0.1
allowed_handoffs: ["customer_support"]
allowed_mcp_servers: ["internal_ledger_mcp"]
evaluator_rubric: "Check the ledger arithmetic."
evaluator_schema_name: "finance"
